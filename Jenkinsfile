pipeline {
	agent any

	stages {

		stage('build') {
			echo "Preparing the build"
		}

		stage('testing') {
			echo "Testing the application"
		}

		stage('Deploy') {
			echo "Deploying the application"
		}
	}
}
