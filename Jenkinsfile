pipeline {
	agent any

	stages {

		stage("build") {
			steps {
				echo "Building the application"
				echo "Build success!"
			}
		}
		stage("testing") {
			steps {
				echo "Testing the application"
			}
		}
		stage("deploy") {
			steps {
				echo "Deploying the application"
			}
		}
	}
}
